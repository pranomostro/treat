#!/usr/bin/env lua

-- analyse the arguments, -s SEP specifies the separator
-- read a line
-- split it up into different fields, using the given separator
-- write each field into the corresponding fifo, the nth+1 argument
